services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - '8080:8080'
    environment:
      PORT: 8080
      DATA_DIR: /data
      TZ: America/Fortaleza
      APP_PASSWORD: ${APP_PASSWORD:-}
    volumes:
      - app-data:/data

volumes:
  app-data:
